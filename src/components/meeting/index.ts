// Meeting notetaker components.
//
// No real UI here yet — that starts in Phase 3 (Historia 1, T017+). This
// barrel follows the same pattern as `src/components/settings/index.ts` and
// `src/components/model-selector/index.ts` so components added later just
// need an export line here.
export {};
