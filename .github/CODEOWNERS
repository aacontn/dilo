# Todo lo que tendría que atravesar un compromiso del canal de actualizaciones.
#
# Dilo corre sin sandbox por necesidad (un tap de CGEvent y las APIs de
# Accesibilidad), así que Sparkle es el único camino influido desde afuera
# hacia un proceso con todos los permisos. Estas rutas deciden qué llave de
# firma se confía, qué dice el feed y qué hace el script de release con las
# dos. Un cambio en cualquiera de ellas lo mira un humano que no sea quien lo
# propone.

/Dilo/Info.plist          @aacontn
/appcast.xml              @aacontn
/scripts/                 @aacontn
/.github/                 @aacontn
