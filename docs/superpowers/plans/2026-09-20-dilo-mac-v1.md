# Dilo Mac v1 — "dictado que parece terminado" — Plan de implementación

> **Para agentes:** una tarea por agente, en el orden de dependencias de abajo. Cada tarea termina con tests verdes, `xcodebuild` limpio en los dos targets y un commit local (sin push). Los pasos usan casillas (`- [ ]`). Se trabaja en `/Volumes/SSD 1/Dilo/mac/` (fork del árbol de origen, MIT); DerivedData en `/Volumes/SSD2/derived-data`. Nada de secretos en el repo. Copy visible en español, autoría propia (tuteo, directo, sin relleno).

**Objetivo:** un Dilo nativo de Mac que dicta en español con motor doble, modos con atajo y proveedor, palabras propias e historial, viviendo en el notch (o en una píldora digna sin notch), firmado y notarizado, con los números del spec medidos en CI. Versión **0.4.0** (continúa la numeración de Dilo; la gente que usa 0.3.2 entiende que es el siguiente).

**Arquitectura:** El árbol de origen se queda como está en `CoreHUD/`, `Dictation/`, `Input/` (cerca de `upstream` para merges baratos). Todo lo de Dilo vive en un Swift Package local **`DiloCore/`** con un módulo por tema (`DiloText`, `DiloEngines`, `DiloModes`, `DiloCapabilities`, `DiloMetrics`), testeable con `swift test` sin abrir Xcode. La app enlaza el paquete una sola vez (Tarea 0); después nadie toca `project.pbxproj` salvo la Tarea 8.

**Stack:** Swift 6, SwiftUI + AppKit, SpeechAnalyzer, **FluidAudio** (Parakeet v3, Apache 2.0), FoundationModels, Sparkle (solo target directo). Sin más dependencias sin justificarlo en el commit.

**Spec:** `docs/superpowers/specs/2026-09-20-dilo-mac-nativo-design.md` — el plan argumenta desde ahí; leer ambos. Los reportes de los spikes están en `docs/superpowers/spikes/`.

## Restricciones globales

- **Dos targets desde la Tarea 0:** `Dilo` (directo, Sparkle) y `Dilo-MAS` (App Sandbox). Todo lo que dependa de Accesibilidad hacia otras apps pasa por `HostCapabilities` (Tarea 2). El sandbox no rompe la compilación: **se prueba en runtime**, lanzando con `open -a`, nunca desde el terminal (TCC atribuye al proceso padre).
- **Motor por defecto: Parakeet v3** (pendiente de la segunda prueba de Alfonso con SpeechAnalyzer en `es_CL`; si gana Apple, es una constante).
- **Gatillos que nunca escriben símbolos** en teclado latino: nada de ⌥ derecha sola. Default: `fn`/🌐 sostenido; alternativa ⌃⌥Espacio.
- **Nunca tocar el volumen maestro.** El ducking heredado queda apagado y oculto; si vuelve, pausa la reproducción.
- **La píldora sin notch va debajo de la barra de menús**, con identidad Dilo (mango, forma de onda, texto parcial). Nunca tapa status items.
- **Los números del spec §3** se miden con `DiloMetrics` en cada tarea que toque arranque, reposo o pegado; si empeoran, no entra.
- Commits en español, imperativo, el porqué; `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Sin push.

## Estructura de archivos nuevos

```
mac/
  AGENTS.md, CLAUDE.md          # propios de Dilo Mac (byte-idénticos, como en app/)
  docs/superpowers/{specs,plans,spikes}/   # copias de los de app/ que apliquen
  DiloCore/Package.swift
  DiloCore/Sources/DiloText/       # muletillas es, diccionario personal
  DiloCore/Sources/DiloEngines/    # SpeechEngine + Apple + Parakeet(FluidAudio)
  DiloCore/Sources/DiloModes/      # Mode, Provider, Decider (reglas)
  DiloCore/Sources/DiloCapabilities/  # HostCapabilities full/sandbox
  DiloCore/Sources/DiloMetrics/    # medición de reposo, arranque, latencia
  DiloCore/Tests/...
  brand/                           # copiado de app/brand
  scripts/metrics.sh               # corre DiloMetrics contra el .app
```

### Tarea 0: Cimientos del repo

**Files:** `AGENTS.md`, `CLAUDE.md`, `docs/superpowers/**`, `DiloCore/Package.swift` (+ un módulo vacío con un test), `Dilo.xcodeproj` (enlazar el paquete; duplicar el target en `Dilo-MAS` con `app-sandbox` + `device.audio-input` + `network.client`), `README.md` (atribución MIT del árbol de origen + la propia).
- [ ] Copiar spec, plan y spikes desde `app/docs/superpowers/` (solo los que apliquen al nativo; los de Rust no).
- [ ] `AGENTS.md` propio: qué es este repo, reglas de arriba, cómo compilar, cómo correr `swift test` y `scripts/metrics.sh`, la regla "CoreHUD/Dictation/Input se quedan cerca de upstream". `CLAUDE.md` idéntico; test que falla si difieren (como en `app/tests/unit/agentInstructions.test.ts`, pero en Swift).
- [ ] Bundle ids: `cl.espaciodigital.dilo` y `cl.espaciodigital.dilo.mas`. Team y firma "Sign to Run Locally" por ahora.
- [ ] `xcodebuild` limpio en los dos targets; `swift test` verde.
- [ ] Commit: `chore: nace Dilo Mac sobre el árbol de origen — dos targets y un paquete propio`

### Tarea 1: Rebrand

**Files:** `Info.plist`, íconos (desde `app/brand/dilo-icon.svg`: generar `.icns` y el ícono de barra de menús), `Assets.xcassets`, `Localizable.xcstrings` (**es como idioma de desarrollo**, en como segundo), nombres visibles, About con atribución.
- [ ] Nombre "Dilo" en todas partes visibles; el producto se llama Dilo, el fork se agradece en About y README.
- [ ] Colores: tinta `#0D1117`, mango `#FF9E1B`, menta `#2EE6A8` (de `landing/styles.css`) como tokens; mango en acciones y en la píldora.
- [ ] Copy en español de todo lo visible de la app (Ajustes, onboarding, HUD, menú). Voz: la del locale `es` de `app/src/i18n/locales/es/translation.json` — leerlo para calcar tono y términos; no traducir literal del inglés.
- [ ] Apagar y ocultar "Duck other audio". Segundo idioma: **Off** por defecto, y su gatillo nunca ⌥ derecha.
- [ ] Commit: `feat(marca): Dilo se ve y habla como Dilo`

### Tarea 2: Capa de capacidades

**Files:** `DiloCore/Sources/DiloCapabilities/`, y los 17 puntos de AX/CGEvent del inventario en `docs/superpowers/spikes/2026-09-20-spike-1-2-sandbox-de-origen.md`.
- [x] Protocolo `HostCapabilities` (foco antes de pegar, releer lo pegado, título de ventana, pegado directo, atajo global, tap de audio). Dos implementaciones: `FullHost`, `SandboxedHost`. Detección por `APP_SANDBOX_CONTAINER_ID`.
- [x] Cada uso heredado de AX pasa por el protocolo. En sandbox, lo que no se puede **se esconde** (patrón TypeMeIt), nunca falla.
- [x] Tests: con `SandboxedHost`, el flujo de pegado usa portapapeles + Cmd+V y no llama AX.
- [x] Verificación en runtime: `open -a Dilo-MAS.app`, pegar en TextEdit; documentar qué prompts de TCC pide.
- [x] Commit: `feat(sandbox): lo que no se puede hacer se esconde, no falla`

**Medido el 2026-09-20** con `/Volumes/SSD2/scratch/dilo-mac/Dilo-MAS-capacidades.app`
lanzada con `open -a`: la detección funciona sola. Primera línea del log —
`log show --predicate 'subsystem == "cl.espaciodigital.dilo"'`—:
`Anfitrión sandbox; escondidas: focoAntesDePegar, relecturaDelFoco, tituloDeVentanaActiva, arrastreDeArchivosAlNotch`.
Arranca sin caerse y sin una sola llamada de Accesibilidad hacia otra app.

TCC, como `cl.espaciodigital.dilo.mas`, pide siete servicios y **los siete
quedan en `Unknown (None)`**, o sea esperando un clic: Micrófono,
Reconocimiento de voz, Accesibilidad (la compuerta del Cmd+V), Input
Monitoring (`kTCCServiceListenEvent`, el tap del gatillo), Captura de audio,
Captura de pantalla y Herramienta de desarrollo (esta última por la firma
ad-hoc). **Pendiente del clic de Alfonso:** conceder Micrófono,
Reconocimiento de voz, Accesibilidad e Input Monitoring a esa entrada nueva de
Ajustes del Sistema, dictar y pegar en TextEdit y en Cursor, y confirmar a ojo
que el menú de la barra no muestra "Leer lo seleccionado" y que Ajustes no
lista "Leer en voz alta". Lo de la Captura de pantalla llama la atención y hay
que mirarlo en la Tarea 6: el HUD no debería necesitarla.

### Tarea 3: Motor doble

**Files:** `DiloCore/Sources/DiloEngines/` (`SpeechEngine` protocol, `AppleEngine`, `ParakeetEngine`), descarga de modelo con progreso, selector en Ajustes con tarjeta LOCAL / EN LÍNEA como en Dilo 0.3.2.
- [x] `SpeechEngine`: `start(locale)`, flujo de parciales, `finish() -> String`. `AppleEngine` envuelve lo que el árbol de origen ya hace.
- [x] `ParakeetEngine` con FluidAudio (Parakeet TDT v3, int8 si existe): al soltar, transcribe el buffer; parciales por trozos de ~2 s si el costo lo permite (medir).
- [x] Modelo se descarga a `~/Library/Application Support/Dilo/models/`, con progreso y cancelación (como el onboarding de Handy). Sin modelo, Apple.
- [x] Tests: contrato con un engine falso; selección persistida; caída a Apple si Parakeet no está.
- [x] Medir con `DiloMetrics`: latencia soltar→texto de cada motor en este M1. Anotar en el plan.
- [x] Commit: `feat(dictado): dos motores detrás de un contrato — Parakeet por defecto`

**Medido 2026-09-20** en este M1 (16 GB, macOS 27) con un dictado de 8,9 s
generado con `say -v Mónica`, 5 repeticiones tras calentar
(`DiloCore/Tests/DiloEnginesTests/MedicionDeMotoresTests.swift`):

| | soltar → texto | qué entregó |
| --- | ---: | --- |
| **Parakeet v3 int8** (buffer entero) | **198 ms** (min 198 · max 206) | el texto con todas sus comas |
| **Apple `SpeechTranscriber`** (audio en vivo, se mide el cierre) | **35 ms** (min 32 · max 76) | el mismo texto sin tres comas |

Los dos pasan los 300 ms del spec §3. Apple llega antes porque transcribe
mientras hablas; Parakeet puntúa mejor y dicta igual en cualquier Mac, que es
por qué sigue siendo el default. **Parciales encendidos:** un trozo de 2 s
cuesta 124 ms, 6 % de ocupación mientras hablas, nunca dos a la vez.

**Modelo:** 469 MB en disco, 23 archivos, ~60 s de descarga con fibra. La
primera compilación del encoder a Core ML cuesta 28 s y pasa una sola vez,
dentro de la descarga.

**Número que se rompió, y cómo se cerró:** el `.app` en Release pasó de
~11 MB a **23 MB** (FluidAudio son ~11 MB de binario + 1 MB de bundle),
contra los 20 MB del spec §3. Se decidió **subir el umbral a 25 MB**:
FluidAudio pesa ~12 MB y es el precio de Parakeet local; en v2 también
diariza, así que podarlo hoy sería trabajo tirado. Yap pesa 4 MB porque sólo
usa Apple; Dilo trae su propio motor.

### Tarea 4: Gatillos y modos

**Files:** `DiloCore/Sources/DiloModes/`, `Input/KeyBindings.swift` (defaults), Ajustes de modos.
- [ ] Portar el diseño de modos de `app/docs/superpowers/specs/2026-07-22-modos-e-ia-design.md`, `2026-07-29-proveedor-por-modo-design.md` y `2026-08-05-atajos-por-modo-design.md`: un modo = nombre + prompt + proveedor + atajo opcional. Proveedores: FoundationModels on-device (sin key), OpenAI, Gemini, Anthropic por API compatible; keys en el Llavero.
- [ ] `Decider` (spec §7) con la implementación de reglas: app al frente → modo; palabras clave desempatan. "Un atajo, Dilo decide" como opción, apagada por defecto.
- [ ] Defaults de gatillo que no escriban símbolos; validador que rechaza ⌥ derecha sola y teclas de volumen.
- [ ] Tests puros: resolución de modo por atajo y por reglas; validador de gatillos.
- [ ] Commit: `feat(modos): cada modo con su atajo y su proveedor, y la app al frente decide si se lo pides`

### Tarea 5: Español de verdad

**Files:** `DiloCore/Sources/DiloText/`, historial, diccionario personal.
- [ ] Portar las reglas de muletillas y limpieza de `app/src-tauri/src/audio_toolkit/text.rs` (leerlas, no copiarlas: son reglas, se reescriben en Swift con los mismos casos de test).
- [ ] Diccionario personal (palabras propias) aplicado como en Handy: como `initial_prompt`/contexto para Parakeet si FluidAudio lo permite, y como post-corrección si no. Multi-palabra.
- [ ] Historial con búsqueda, copiar, borrar, y "el modo con que se dictó".
- [ ] Tests con dictados chilenos reales de estilo (muletillas, voseo, spanglish técnico intacto).
- [ ] Commit: `feat(es): muletillas fuera, spanglish intacto, y tus palabras se respetan`

### Tarea 6: Píldora y notch con identidad

**Files:** `CoreHUD/HUDNotchGeometry.swift`, `HUDPlacement.swift`, un visual nuevo en `Dictation/HUD/Visuals/`.
- [ ] Sin notch: píldora **debajo** de la barra de menús (`hudClearsMenuBar` a 1 por defecto en pantallas sin notch), mango, forma de onda y texto parcial. Con notch: la forma heredada con los colores de Dilo.
- [ ] Nivel de ventana que sobreviva a apps en pantalla completa (issue #84 del árbol de origen; ver cómo lo hace `app/src-tauri/src/overlay.rs`).
- [ ] Tres estados previstos en la máquina (dictando, reunión, conversando); solo el primero se dibuja en v1.
- [ ] Commit: `feat(hud): la píldora es de Dilo y no tapa nada`

### Tarea 7: Números no negociables

**Files:** `DiloCore/Sources/DiloMetrics/`, `scripts/metrics.sh`.
- [x] Medir: RAM en reposo a los 60 s, CPU en reposo (60 s), arranque en frío, latencia soltar→texto (con un WAV fijo inyectado), tamaño del `.app` sin modelos.
- [x] Umbrales del spec §3 como test que falla: 60 MB, ~0 %, 1 s, 300 ms, 25 MB.
- [x] Commit: `test(metrics): los números del spec se miden, no se prometen`

**Medido el 2026-09-20**, Mac mini M1 · macOS 27.0, con `./scripts/metrics.sh`
contra `Dilo.app` Debug de esta rama (la de la Tarea 1 más el gancho), y con
otros cuatro agentes compilando en la misma máquina:

| Métrica | Medido | Umbral | Cómo |
| --- | --- | --- | --- |
| RAM en reposo | **18,8 MB** | < 60 MB | `footprint` → `phys_footprint`, a los 60 s de arrancar, sin dictar |
| CPU en reposo | **0,05 %** | ~0 % (tolerancia: < 1 %) | CPU consumida / tiempo, ventana de 60 s, vía `proc_pid_rusage` |
| Arranque en frío | **119 ms** | < 1 s | `open -n -a` → `isFinishedLaunching`, mediana de 5 (112/116/119/145/186) |
| Soltar → texto | **90 ms** | < 300 ms | WAV de 5 s dicho por `say`; de `MicrophoneInput.stop()` al portapapeles |
| `.app` sin modelos | **17,3 MB** | < 25 MB | bytes del bundle en disco (Debug con `.debug.dylib`) |

El umbral del `.app` es **25 MB desde el merge del motor doble**, no los
20 MB con que se midió esta tabla: FluidAudio pesa ~12 MB y es el precio de
Parakeet local; en v2 también diariza, así que podarlo hoy sería trabajo
tirado. Yap pesa 4 MB porque sólo usa Apple; Dilo trae su propio motor. Los
números medidos acá no cambian; el techo sí.

El `.app` de la Tarea 1 tal cual (`/Volumes/SSD2/scratch/dilo-mac/Dilo.app`,
sin el gancho) da los mismos números en las cuatro métricas que no lo
necesitan: **18,7 MB / 0,04 % / 195 ms / 12,9 MB**. Es 4 MB más liviano porque
no arrastra el `.debug.dylib` de esta build.

Lo que la medición **no** dice: el arranque es con caché de archivos caliente
—vaciarlo pide `sudo purge` o un reinicio—, y la latencia es la segunda pasada,
con el modelo de español ya cargado. La primera pasada de la sesión paga la
carga del modelo, que es disco y no Dilo.

El reporte queda en `docs/metricas/ultima-medicion.json`, versionado a
propósito: `swift test` lo lee y falla si un número se rompe.

### Tarea 8: Firma, notarización, updater, MAS

**Files:** `.github/workflows/`, `Sparkle` (solo `Dilo`), entitlements.
- [ ] Portar la cañería de `app/.github/workflows/build.yml` (Developer ID, notarización con `APPLE_API_*`; los secrets existen en `aacontn/dilo`). Notarización como paso propio con reintentos (el fallo del 14-sep fue red al sondear).
- [ ] `Dilo-MAS` compila y se firma para App Store; la publicación es decisión aparte.
- [ ] Appcast propio para Sparkle.
- [ ] Commit: `ci: firmado, notarizado y con updater — como se prometió en el README`

### Tarea 9: Onboarding y 0.4.0

- [x] Onboarding en español: permisos (Micrófono, Reconocimiento de voz, Accesibilidad, Input Monitoring) con el porqué de cada uno; elegir motor; probar un dictado.
- [x] Release notes 0.4.0 dentro de la app, en español.
- [x] `scripts/metrics.sh` verde. [ ] Alfonso dicta diez minutos. Si pasa, tag `v0.4.0`.
- [x] Commit: `chore(release): 0.4.0 con build 1 — Dilo nativo de Mac`

**Cómo quedó el onboarding.** Cuatro pantallas en `Dilo/Onboarding/`: la
bienvenida con el gatillo que de verdad está configurado ("Aprieta, habla,
suelta. Ya está escrito."), los permisos uno por uno con su porqué y su botón
al panel exacto de Ajustes del Sistema, el mismo `SelectorDeMotor` de Ajustes
con la descarga de Parakeet, y un dictado de prueba en un campo de la propia
ventana. Se abre solo la primera vez y queda en el menú como **Primeros
pasos…**. Qué permisos se piden lo decide `Permiso.pasos(anfitrion:motorEsApple:)`
en `DiloCapabilities`, con tests: lo que el anfitrión esconde no se pide y el
Reconocimiento de voz sólo aparece con el motor de Apple elegido. Las notas de
versión viven en `Dilo/Resources/NotasDeVersion/0.4.0.md` —un solo archivo
que la app muestra en Ajustes → Novedades y que `scripts/release.sh` publica—.

**Medido el 2026-09-20** en el Mac mini M1 (Macmini9,1, macOS 27.0). Cuatro
números sobre el `.app` **Debug** —es el único que trae el gancho
`DILO_METRICS_WAV`— y el tamaño sobre su gemelo **Release**, que es el que se
descarga: `./scripts/metrics.sh <Debug>.app --tamano-de <Release>.app`, bandera
nueva de esta tarea.

| Métrica | Medido | Umbral | ¿Pasa? | Cómo |
| --- | --- | --- | --- | --- |
| RAM en reposo | 46.4 MB | < 60 MB | sí | footprint phys_footprint, 60 s tras arrancar, sin dictar |
| CPU en reposo | 0.05 % | < 1 % | sí | tiempo de CPU consumido / tiempo transcurrido, ventana de 60 s |
| Arranque en frío | 157 ms | < 1000 ms | sí | open -n -a → NSRunningApplication.isFinishedLaunching, mediana |
| Soltar → texto | 258 ms | < 300 ms | sí | WAV de 5 s inyectado; de MicrophoneInput.stop() al portapapeles |
| .app sin modelos | 23.4 MB | < 25 MB | sí | bytes del bundle en disco (Release) |

Dos cosas que cambiaron desde la medición de la Tarea 7 y que conviene mirar
antes de v1.1:

1. **La RAM en reposo pasó de 18.8 MB a 46.4 MB.** No es el onboarding: es que
   ahora el modelo de Parakeet está descargado y el motor por defecto lo carga
   al arrancar. Sigue bajo el umbral del spec, pero el margen se achicó de
   41 MB a 14. Con el motor de Apple elegido vuelve a la cifra vieja.
2. **La primera sesión tarda en estar lista.** Con Parakeet y su modelo en
   disco, Core ML se toma unos veinte segundos en un M1 ocioso antes de que el
   dictado responda. La medición de latencia lo espera (40 intentos); una
   persona que aprieta la tecla a los cinco segundos de arrancar, no.

**Las dos ya están corregidas en `fix/motor-perezoso`** y **los 46.4 MB de la
tabla quedan obsoletos: el número de reposo se re-mide en CI con esta
corrección puesta.** El modelo de Parakeet ya no entra a la RAM al arrancar la
app ni al abrir Ajustes u onboarding —`prewarm` dejó de cargarlo—, sino al
empezar el primer dictado y en paralelo a la grabación; si la persona suelta
antes de que esté listo, el buffer espera al modelo y la píldora dice
"Cargando el modelo…". Y tras `descargarModeloTras` sin dictar (Ajustes →
Motor, 5 minutos por defecto, con opción "nunca") el modelo se suelta de la
memoria, como el *Unload Model* de Handy. Medido con
`MedicionDeMotoresTests.cargaEnFrioDelModelo`: cargarlo cuesta 172–412 ms más
195 ms de la primera predicción —menos que un dictado corto—, salvo la primera
vez después de la descarga, que son los ~28 s en que Core ML compila el
encoder. La regla se prueba en `swift test`
(`CargaPerezosaTests`), no a ojo.

El `.app` Release de 0.4.0 quedó en
`/Volumes/SSD2/scratch/dilo-mac/Dilo-0.4.0.app`.

## Orden y paralelismo

`0 → 1` en serie. Después, en paralelo en worktrees: `2`, `3`, `4+5`, `6`, `7`. `8` cuando `2` y `3` estén. `9` al final. Cada rama se audita y se mergea a `main` del repo `mac/`.

## Addendum 2026-09-21 — cierre antes del tag `v0.4.0`

Del diagnóstico de Astra (`docs/design/2026-09-21-experiencia-dilo.md`) y de la
primera prueba de Alfonso salen cinco tareas que van **antes** del tag. Cada una
en su rama y worktree; se integran a `main` como las demás.

- [ ] **10 · Un único sistema de modos** (`feat/modos-unificados`): migración
      idempotente de `shapingPrompts` a `Modo`, gatillos por modo funcionales y
      validados contra todos los demás, proveedor congelado por sesión, nunca
      fallback silencioso local→remoto, "un atajo, Dilo decide" enchufado o
      escondido, historial con el modo real.
- [ ] **11 · Resultado recuperable** (misma rama): original y transformado
      separados, "Copiar el último dictado" en el menú, pegado fallido → portapapeles.
- [ ] **12 · Cualquier tecla sola como gatillo** (`fix/teclas-libres`): F13–F20 y
      teclas sin leyenda; Alfonso no podía asignar F18/F19 en su Apple Keyboard.
- [ ] **13 · Notch simulado opcional** (`feat/notch-simulado`): en pantallas sin
      notch, elegir entre píldora bajo la barra (default) y notch simulado; en su
      Mac mini "no se ve como notch".
- [x] **14 · Retirar los recursos heredados del bundle** (`chore/recursos-heredados`):
      pasada de recursos + comprobación del Release. Ocultar no es retirar.
      **Alcance recortado en marcha por Alfonso**: los estilos visuales (Siri Wave,
      orbe Siri, todas las ondas del picker) y todos los sonidos que el picker
      ofrece se quedan como opciones activas; se retira sólo lo que no referencia
      ningún código ni ajuste. Retirado: `siri-shadow.imageset` (1,9 MB de PDF que
      `HUDSiriOrbView` nunca dibuja), `appicon.png` y `menubar-appicon.png` de la
      raíz, y la línea `/Casks/` de CODEOWNERS. El `.app` Release baja de
      **24 424 KB a 22 004 KB** (`Assets.car`, de 5,2 MB a 2,7 MB). Siguen dentro,
      por decisión explícita: los WAV de Pop (CC-BY-NC, `DictationSoundSet.pop` los
      referencia y el picker Debug los ofrece — **siguen bloqueando un release
      comercial**) y `WaveformSheen.metal` (se conserva a propósito, ver
      `HUDShaderWarmUp`). El catálogo no tiene ni una clave sin uso.
- [ ] **15 · Sonidos Pop fuera** (`fix/sonidos-pop`): son CC-BY-NC (freesound,
      "Plunger pop 1") y el propio `LICENSE-SOUNDS.txt` los marca como placeholder
      a retirar antes de cualquier release. No es gusto, es licencia: se retira el
      caso `.pop` y sus WAV; los demás juegos (Click CC0, Chime y Synth propios del
      árbol de origen, MIT) se quedan.

Verificación de cierre: CI verde en `main`, y diez minutos de dictado de Alfonso.
